---
name: srt-whiteboard-animation
description: SRT 자막을 따뜻한 미색 종이 배경의 화이트보드 손그림 애니메이션으로 만듭니다. 자막 읽기 → 삽화 전략 출력 → 확인 후 통일된 스타일의 선화 생성 → 내러티브 의미에 따라 영역 주석 작성 → 프리뷰 스튜디오에서 조정 → MP4 렌더링. 오케스트레이션은 영역별 마스크 공개 방식(annotation.json / sequence / startMs / protectedRegions)을 그대로 사용하되, 각 영역 내부의 드로잉은 stream 방식의 연속적인 필기(스켈레톤/그리드 ink→color)로 대체합니다. 사용자가 SRT 자막을 제공하며 "자막을 화이트보드 손그림/스트리밍 필기 영상으로 만들어줘", "SRT로 화이트보드 애니메이션 만들기", "자막에 맞춰 장면별로 손그림 그리기"(또는 "turn subtitles into a whiteboard hand-drawn video", "generate whiteboard animation from SRT", "draw scenes from subtitles")라고 요청할 때 발동합니다.
---

# SRT 화이트보드 애니메이션 (mask 오케스트레이션 + stream 드로잉)

SRT 자막을 화이트보드 손그림 애니메이션으로 변환합니다. **오케스트레이션**은 영역별 마스크 공개 방식을 그대로 사용합니다(내러티브 순서대로 영역을 하나씩 공개, 시작하지 않은 영역은 완전히 숨김, 겹치는 부분은 `protectedRegions`로 보호). **드로잉**은 스트리밍 필기 방식으로 대체합니다 — 각 영역은 자신의 허용 마스크 안에서 펜촉이 스켈레톤/그리드를 따라 연속적으로 미끄러지며 선을 긋고(선화 ink → 채색 color), 모든 영역은 하나의 영속 캔버스를 공유하여 이미 그려진 영역은 캔버스에 그대로 남습니다. 모든 사용자 대상 설명, 스토리보드, 설정, 인터페이스 텍스트는 반드시 한국어를 사용해야 합니다.

프레임 단위로 튀듯 나타나거나 사각형으로 지우는 방식과 달리, 이 Skill의 필기는 **연속적으로 흐릅니다**. 이미지 전체를 대상으로 하는 stream 방식과 달리, 이 Skill은 **자막의 내러티브에 따라 영역별로** 차례차례 그리므로 각 요소의 등장 순서와 타이밍을 제어할 수 있습니다.

## 기본 구현 파라미터

| 항목 | 기본 요구 사항 |
|---|---|
| 종이 배경 | 생성 이미지는 따뜻한 미색 옛 종이색 사용(권장 `#F5EBD7`). 렌더링 시 원본 이미지의 네 모서리에서 안쪽으로 들어간 지점을 샘플링해 배경을 물들이며, 순백색은 금지. |
| 드로잉 방식 | 영역마다 stream 연속 필기: 선화 `ink`(선 깔기) → 채색 `color`(원본 색상 복원). 가중치 `ink:color = 2:1`. |
| 필기 경로 | `--ink-path grid`(그리드, 기본값, 안정적) 또는 `skeleton`(스켈레톤 추적, 선화가 선명한 삽화에 더 잘 맞음). |
| 채색 스타일 | `--color-fill contour-wipe`(윤곽 스캔, 기본값) 또는 `brush`(궤적을 따라 칠하기). |
| 미완성 영역 | 영역의 허용 마스크 = 사각형 `region`에서 「후속 영역 + protectedRegions」를 제외한 부분. 시작하지 않은 영역은 완전히 숨김. |
| 시간 산정 기준 | 각 이미지의 `sceneDurationMs`는 해당 장면 자막의 시간 범위에서 가져옴(권장 25–35초/장면). |
| 편집 박스 | 프리뷰 스튜디오는 기본적으로 번호가 매겨진 모든 편집 박스를 표시. 편집 박스는 애니메이션 화면 콘텐츠에 포함되지 않음. |

## 통일된 이미지 생성 비주얼 규범 (필수)

모든 장면의 원본 이미지는 동일한 비주얼 언어를 따라야 합니다. 생성 전에 아래 요구 사항을 이미지 생성 프롬프트에 빠짐없이 기재하고, 생성 후 항목별로 점검하세요.

- **스타일과 구도:** 미니멀한 손그림 삽화, 순수 스케치 스타일, Notion 같은 절제된 낙서 미학. 사실성보다 개념 표현에 중점을 두며, 구도는 간결하게, 배경은 깨끗하게, 여백은 넉넉하게. 전체적으로 차분하고 명료한 정서를 유지하고, 시리즈 내에서 선·인물·색감이 일관되어야 합니다.
- **색상과 재질:** 미색 종이 배경 `#F5EBD7`, 짙은 회색 스케치 선. 빨강, 주황, 파랑만 소량의 개념적 포인트로 사용할 수 있습니다. 그 외의 강조색, 고채도 배색, 복잡한 텍스처는 사용할 수 없습니다.
- **인물과 사물:** 간결한 윤곽, 최소한의 선, 여백으로 대상을 표현하며, 실제 비율·재질·디테일이 아니라 관계/변화/핵심 개념을 강조합니다.
- **절대 금지:** 장면 원본 이미지 안의 모든 문자, 단어, 알파벳, 숫자, 서체, 라벨. 사실적 느낌, 사진적 디테일, 3D 효과, 회화적 질감. 복잡한 장면, 빽빽한 배경, 번잡한 장식, 고채도 화면.
- **드로잉 손 예외 조항:** 사용자가 펜대에 적힌 문자가 자신의 로고이며 유지해 달라고 명확히 밝힌 경우에는 `drawing-hand.png` 펜대의 로고를 남겨 둘 수 있습니다. 이것은 장면 원본 이미지의 문자에 해당하지 않으며, 지우거나 다시 그릴 필요가 없습니다. 사용자의 명확한 설명이 없으면 여전히 문자가 없는 화면으로 처리합니다.

## 확인 관문 (필수)

기본 워크플로우의 **매 단계가 끝날 때마다 반드시 멈추고 사용자의 명확한 확인을 기다린 뒤**에야 다음 단계를 시작할 수 있습니다. 확인 전에는 다음 단계의 이미지, 주석, 프리뷰, 영상, 병합 파일을 생성해서는 안 됩니다. "무응답", "이전의 포괄적 승인", "사용자가 반대하지 않음"을 확인으로 간주해서는 안 됩니다. 사용자가 이전 단계의 수정을 요청하면 해당 단계만 다시 수행하고, 완료 후 다시 확인을 기다립니다.

유일한 연계 동작은 다음과 같습니다. **주석 JSON 생성이 완료되면 즉시 자동으로 프리뷰 스튜디오를 열고 해당 JSON이 있는 디렉터리를 로드해야 합니다.** 이는 3단계의 산출물에 포함되므로 "프리뷰 스튜디오 열기"를 위해 별도로 확인을 기다릴 필요가 없습니다. 브라우저의 File System Access API가 사용자 제스처를 요구하는 경우, 브라우저 UI에서 이미 확정된 그 디렉터리를 선택하세요. 이를 이유로 사용자에게 추가 확인을 요구하거나 사용자가 직접 프리뷰 스튜디오를 열도록 미뤄서는 안 됩니다.

## 워크플로우

1. **자막 읽기, 전략 수립 (이미지 생성 없음).** `scripts/parse_srt.py`로 SRT를 자막 항목으로 파싱하고 25–35초/장면 기준으로 권장 스토리보드를 제시합니다. 이를 바탕으로 삽화 전략을 출력합니다: 각 장면의 번호, 핵심 표현, 화면 주체, 대응 자막 구간, `sceneDurationMs`. 각 장면은 하나의 핵심 의미만 표현합니다. **완료 후 멈추고 사용자의 전략 확인을 기다립니다.**
2. **선화 생성.** 사용자가 전략을 확인한 뒤에만, "통일된 이미지 생성 비주얼 규범"에 따라 장면마다 16:9 따뜻한 미색 옛 종이 배경의 선화 이미지를 생성합니다. 배경은 `#F5EBD7`이며, 주체 사이에는 자동 분할이 쉽도록 충분한 여백을 둡니다. 문자, 복잡한 사진, 겹치는 객체, 규범과 충돌하는 요소를 생성해서는 안 됩니다. **완료 후 멈추고 선화를 보여준 뒤 사용자의 확인을 기다립니다.**
3. **자막을 먼저 읽고 이미지를 본 뒤 주석을 작성하고 프리뷰 스튜디오를 엽니다.** 사용자가 선화를 확인한 뒤에만, 먼저 해당 이미지에 대응하는 자막을 읽고, 실제로 이미지를 확인하고, 원본 이미지의 픽셀 너비·높이를 확보합니다. 자막만으로 화면을 추측해서도 안 되고, 화면상의 위치만으로 기계적으로 순서를 정해서도 안 됩니다. 먼저 자막의 내러티브 이벤트를 추출한 다음, 이미지에서 보이는 주체를 각 이벤트에 대응시키고, "장면 설정 → 핵심 인물/사물 → 동작 갈등 또는 변화 → 반응/결과"의 의미적 순서로 드로잉을 배치합니다. 그다음 `<이미지명>.annotation.json`을 생성합니다. 생성이 완료되면 즉시 기본 브라우저로 `assets/preview.html`을 열고, 프리뷰 스튜디오의 "폴더 열기"를 통해 **해당 주석 파일이 있는 디렉터리**의 모든 `<이름>.png` + `<이름>.annotation.json`을 로드합니다. 파일 경로만 알려주거나 사용자가 직접 조작하도록 요구해서는 안 됩니다. **프리뷰 스튜디오에 디렉터리가 로드된 뒤 멈추고, 사용자의 주석 및 프리뷰 내용 확인을 기다립니다.**
4. **영역 검사 이미지 생성.** 사용자가 주석과 프리뷰 내용을 확인한 뒤에만, `render_annotation_preview.py`로 번호/방향 검사 이미지를 만들고, 영역 분할이 내러티브 순서와 일치하는지, 모든 영역이 캔버스 안에 있는지, 겹치는 주체가 `protectedRegions`로 보호되는지 확인합니다. **완료 후 멈추고 사용자의 검사 이미지 확인을 기다립니다.**
5. **프리뷰 스튜디오에서 조정하고 저장.** 사용자가 검사 이미지를 확인한 뒤에만, 이미 열려 있고 해당 디렉터리가 로드된 프리뷰 스튜디오에서 조정합니다. 기본 상태(미재생)에서는 전체 이미지와 영역 박스를 표시합니다. 캔버스는 **사각형 프록시**입니다: 영역의 네 변과 네 모서리를 드래그해 `region`을 변경하고, 오른쪽에서 이름/방향/**시작(ms)/종료(ms)**(길이 = 종료−시작, 읽기 전용)와 **자막**을 수정하며, 모듈 목록을 드래그해 **순서를 조정**합니다(`sequence` 자동 재정렬). 모듈을 선택하면 대응 자막이 자동으로 하이라이트됩니다. 타임라인을 드래그하거나 재생 버튼을 눌러 공개 과정을 확인할 수 있습니다(시작하지 않은 영역은 표시되지 않음). `direction`은 이 프록시에만 영향을 줍니다. 수정을 마치면 "이 장면 저장/전체 저장"을 눌러 원본 `.annotation.json`에 기록합니다(영역별 `subtitle` 포함, `sceneDurationMs`는 마지막 영역 종료 시각 +0.5초에 맞춤). **저장 후 멈추고 사용자의 최종 주석 및 타이밍 확인을 기다립니다.**
6. **명령줄에서 최종 영상 렌더링.** 사용자가 최종 주석과 타이밍을 확인한 뒤에만, `render_stream_whiteboard.py`로 장면마다 고화질 MP4를 만들고, 오프닝·겹치는 모듈의 중간 지점·엔딩 세 시점을 샘플 검사합니다. **완료 후 멈추고 사용자의 최종 영상 확인을 기다립니다.**
7. **다중 장면 병합 (다중 장면인 경우에만).** 사용자가 모든 단일 장면 영상을 확인한 뒤에만, `merge_scenes.py`로 순서대로 하나의 영상으로 병합합니다. **완료 후 멈추고 사용자의 최종 합성 영상 확인을 기다립니다.**

## 디렉터리 규약

사용자 프로젝트 안에 생성합니다:

```text
assets/whiteboard/<프로젝트명>/
  scene-01-<이름>.png
  scene-01-<이름>.annotation.json     # png와 같은 이름
  scene-01-<이름>-whiteboard.mp4      # 최종 영상
  scene-01-<이름>-preview.mp4         # 실제 클립 (프리뷰 스튜디오 생성, 저화질)
```

이미지와 설정 파일은 반드시 같은 이름이어야 합니다: `foo.png`는 `foo.annotation.json`에 대응합니다. 프리뷰 스튜디오는 이를 기준으로 설정을 자동 로드합니다.

## 의미적 정렬과 픽셀 단위 주석 (반드시 실행)

1. **판단 근거:** 주석 작성 전에 반드시 자막과 실제로 확인한 원본 이미지를 모두 갖추어야 합니다. 둘 중 하나라도 없으면 먼저 요청하고, 주석을 생성해서는 안 됩니다.
2. **순서 근거:** `sequence`, `startMs`, `label`은 자막 속 사건의 선후 관계를 반영해야 하며, 단순히 왼쪽에서 오른쪽, 위에서 아래, 또는 시각적으로 두드러지는 정도에 따라 정해서는 안 됩니다.
3. **좌표 근거:** 각 모듈은 원본 이미지 좌표계의 정수 픽셀 `x`, `y`, `width`, `height`를 출력합니다. 원점은 좌측 상단이며, 백분율/비율/추정 좌표나 크기 생략은 금지합니다. `canvas.width`/`canvas.height`는 반드시 원본 이미지의 픽셀 크기와 같아야 합니다.
4. **모듈 필드:** 각 요소는 `sequence`, `narrativeRole`, `subtitle`, `region`, `reveal`, `handPath`를 포함합니다. `narrativeRole`은 자막 속에서의 내러티브 역할을 한국어로 설명합니다. `subtitle`에는 해당 영역에 대응하는 자막 텍스트(SRT 출처)를 저장하며, 프리뷰 스튜디오 연동과 이후 활용에 사용됩니다. `sequence`는 1부터 연속됩니다.
5. **검증:** 프리뷰 생성 전에 각 영역이 캔버스 안에 있는지, 대응하는 가시 주체를 덮고 있는지, 자막 사건과 부합하는지 확인합니다. 겹치는 주체는 `protectedRegions`로 나중에 그릴 모듈을 보호합니다.

## 타이밍 모델 (stream 드로잉 전용)

- **장면 총 길이** `sceneDurationMs`는 해당 장면 자막의 시간 범위에서 가져옵니다(`parse_srt.py`의 `scenes[].sceneDurationMs`).
- **영역 직렬 드로잉:** stream 드로잉은 펜 한 자루가 움직이는 방식이므로, 같은 장면 안의 각 영역은 **시간상 차례대로 진행**되어야 합니다(`startMs` 겹침 없음). 다음 영역은 이전 영역의 `startMs + durationMs`(+ 선택적으로 100–300ms의 호흡)부터 시작합니다.
- **겹침 처리:** `startMs`가 겹치면 렌더러는 영역을 순서대로 이어 붙이되, 장면이 `sceneDurationMs`를 넘지 않도록 각 영역의 길이를 비례 축소하고 경고를 출력합니다. 장면이 길어지면 병합 후 뒤따르는 모든 장면이 내레이션과 어긋나기 때문입니다. 축소 경고가 보이면 프리뷰 스튜디오에서 겹친 구간을 풀어 주세요.
- **드로잉 순서의 기준은 `sequence`:** 렌더러는 `sequence` 순서대로 그리며, 가림(occlusion) 계산도 같은 순서를 씁니다. 프리뷰 스튜디오에서 모듈 순서를 바꾸면 `sequence`만 갱신되고 `startMs`는 그대로 남기 때문입니다. 두 값이 어긋나면 렌더러가 경고를 출력하고 `startMs`를 무시한 채 영역을 이어 붙입니다.
- **영역 내 ink→color:** 각 영역의 `durationMs`는 `ink:color = 2:1` 비율로 선화 구간과 채색 구간으로 나뉩니다. `durationMs`는 프리뷰 스튜디오의 **시작/종료 시각**으로 결정되며(종료−시작), 해당 영역에 대응하는 자막의 길이에 맞출 수 있습니다. 초기 추정값으로는 초당 150픽셀 × 드로잉 거리를 사용할 수도 있습니다.
- **응시 마무리:** 모든 영역을 그린 뒤 자동으로 `sceneDurationMs`까지 채우며, 마지막에 완전한 원본 이미지가 최소 0.5초 유지되도록 보장합니다.
- `reveal.direction`은 stream 드로잉에서 **실제 필기를 결정하지 않으며**(필기는 스켈레톤/그리드로 자동 생성), 프리뷰 스튜디오의 사각형 프록시 시연용으로만 쓰입니다. 프리뷰 스튜디오를 사용할 수 있도록 남겨 둔 필드입니다.

## 마스크 불변식 (오케스트레이션 계층, 반드시 실행)

- 시각 `t`에서 모듈은 `reveal.startMs ≤ t` 이후이면서 현재 드로잉 진행도를 넘지 않는 픽셀만 표시할 수 있습니다. 시작하지 않은 모듈의 선, 채움, 이미지는 어떤 것도 나타나서는 안 됩니다.
- 각 영역의 **허용 마스크** = 사각형 `region`에서 모든 **후속 모듈의 `region`**을 제외하고, 다시 해당 모듈의 `reveal.protectedRegions`를 제외한 영역입니다. stream 드로잉은 허용 마스크 안으로 제한되므로 후속 영역의 선이 미리 드러나지 않습니다.
- `protectedRegions`는 `region`과 동일한 원본 이미지 정수 픽셀 좌표를 사용하며, 사각형이 지나치게 크거나 주체가 겹치거나 배경 선이 새어 나올 수 있는 경우에 사용합니다.
- 렌더러는 "허용 마스크 안으로 드로잉을 제한 → 후속 영역과 보호 영역은 자연스럽게 건드리지 않음" 순서를 이미 구현하고 있습니다. 프리뷰 스튜디오의 사각형 프록시는 이와 동등한 `destination-out` 제외 방식으로 같은 오케스트레이션을 시연합니다.

## 설정 예시

```json
{
  "sceneId": "scene-01",
  "canvas": { "width": 1672, "height": 941 },
  "storyBasis": "해당 장면 자막의 사건 요약",
  "sceneDurationMs": 9000,
  "elements": [
    {
      "id": "rockery",
      "label": "석가산 장면",
      "sequence": 1,
      "narrativeRole": "이야기의 장면 설정",
      "subtitle": "원숭이 산에서 아기 원숭이 한 마리가 석가산 꼭대기에 앉아 손에 바나나를 들고 있다.",
      "type": "structure",
      "region": { "x": 20, "y": 120, "width": 540, "height": 780 },
      "reveal": { "direction": "top_to_bottom", "startMs": 300, "durationMs": 2600, "maskPaddingPx": 22, "protectedRegions": [] },
      "handPath": { "start": [290, 130], "end": [290, 890], "easing": "easeInOut" }
    }
  ]
}
```

> `direction` / `handPath`는 프리뷰 스튜디오의 사각형 프록시 전용입니다. 최종 영상의 필기는 stream이 자동 생성하므로 세밀하게 조정할 필요가 없습니다.

## 스크립트 사용법

모든 렌더링 스크립트는 Skill 내부 `.venv`의 인터프리터로 실행합니다(의존성 격리).

1. **환경 준비** (최초 실행 또는 의존성 누락 시):
   ```bash
   python scripts/prepare_env.py --check   # 탐지. 성공 시 마지막 줄에 ENV_PY=<경로> 출력, 캡처해 두고 사용
   python scripts/prepare_env.py           # 없으면 .venv 생성 후 opencv-python/numpy/av 설치
   ```
2. **자막 파싱 + 스토리보드 제안**:
   ```bash
   python scripts/parse_srt.py <자막.srt> --target-sec 30 --min-sec 25 --max-sec 35
   ```
3. **영역 번호 검사 이미지**:
   ```bash
   <ENV_PY> scripts/render_annotation_preview.py <이미지> <주석> <검사 이미지 출력>
   ```
4. **프리뷰 스튜디오 (서버 불필요)**: Chrome / Edge로 `assets/preview.html`을 직접 열고, "폴더 열기"를 눌러 디렉터리를 선택 → 모든 이미지 + 같은 이름의 주석 로드 → 드래그로 편집 → "저장"으로 원본 파일에 기록. 기록에는 File System Access API가 필요합니다(Chrome/Edge). 다른 브라우저에서는 다운로드 후 수동으로 덮어쓰세요. 렌더링은 여전히 명령줄에서 진행합니다(아래 5단계).
5. **단일 장면 최종 영상 렌더링**:
   ```bash
   <ENV_PY> scripts/render_stream_whiteboard.py <이미지> <주석> <출력mp4> assets/drawing-hand.png \
       [--ink-path grid|skeleton] [--color-fill contour-wipe|brush] [--total-ms <밀리초>]
   ```
   `--total-ms`를 생략하면 주석의 `sceneDurationMs`를 사용합니다. 마지막 줄에 `OUTPUT=<경로>`를 출력합니다.
6. **다중 장면 병합**:
   ```bash
   <ENV_PY> scripts/merge_scenes.py --inputs 장면1.mp4 장면2.mp4 장면3.mp4 --output final.mp4
   ```

## 품질 검사

렌더링 전/후에 확인합니다:

- 첫 프레임이 깨끗한 따뜻한 미색 옛 종이 배경이며, 선이 미리 드러나지 않았는가.
- 대응하는 자막을 읽고 원본 이미지를 실제로 확인했는가. `canvas`가 원본 이미지의 픽셀 크기와 일치하고, 모든 `region`이 정수 픽셀 좌표이며 캔버스 안에 있는가.
- `sequence`, `startMs`가 자막 사건 순서와 일치하는가. 검사 이미지의 번호/라벨/영역이 동일한 주석 JSON에서 나왔는가.
- 오프닝, 겹치는 모듈의 중간 지점, 모든 모듈 완료 후 세 시점에서 확인: 아직 그리지 않은 모듈은 모두 보이지 않고, 겹침 보호 영역이 새어 나오지 않으며, 최종 프레임은 완전한 원본 이미지를 표시하는가.
- 펜촉이 진행 중인 필기에 밀착해 있는가. 선화가 선명한 삽화는 `--ink-path skeleton`으로 필기를 더 잘 맞출 수 있습니다.
- 모든 모듈이 끝난 뒤 완전한 원본 이미지가 최소 0.5초 유지되는가.
- 다중 장면 병합 후 순서와 길이가 자막 스토리보드와 일치하는가.

효과를 수정하려면 먼저 프리뷰 스튜디오(`assets/preview.html`)에서 주석(영역/순서/타이밍)을 조정하고 저장한 뒤 명령줄에서 렌더링하세요. 근거 없이 반복해서 영상을 뽑지 마세요.
